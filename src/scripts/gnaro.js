export const makeChoice = bullet => {
  if(bullet)
    return "¡Dispara!";
  else if(!bullet)
    return "Sacando el Fierro";

  // let prob = getRandomInt(0, 100);

  // if(prob > 10 && prob <= 50  && !bullet) {
  //   return "Sacando el Fierro";
  // }
  // else if(prob >= 0 && prob < 50)
  // return "Golpe Duro";

  // else if(prob >= 50 && prob <= 75)
  //   return "Tramontina";
  // else if (prob > 75)
  //   return "Rompe Cuellos";

}
